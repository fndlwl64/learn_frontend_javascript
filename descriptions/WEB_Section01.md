# 웹 브라우저 동작 과정

## 1. URL 입력
- 사용자가 브라우저 주소창에 URL 입력  
  예: `https://www.example.com/index.html`  
- URL 구성 요소:
  - **프로토콜**: `https`
  - **도메인**: `example.com`
  - **경로**: `/index.html`

---

## 2. DNS 조회
- 브라우저는 캐시(브라우저, OS, 라우터 등)에서 IP 주소 확인  
- 없으면 **DNS 서버**에 요청 → 도메인(`example.com`)을 IP 주소(`93.184.216.34`)로 변환

---

## 3. TCP 연결 (3-way handshake)
- IP 주소를 얻은 후 서버와 **TCP 연결** 수립  
- 안정적인 데이터 전송을 위해 **3-way handshake** 수행

---

## 4. HTTP/HTTPS 요청
- 브라우저가 **HTTP 요청 메시지** 생성  
  - 예: `GET /index.html HTTP/1.1`  
- HTTPS인 경우, 먼저 **TLS 핸드셰이크**로 암호화된 연결 설정  
- 요청 메시지는 TCP를 통해 서버로 전송

---

## 5. 서버 처리
- 서버는 요청을 받아 해당 리소스(`index.html`) 검색  
- 동적 페이지라면 DB 조회, 백엔드 로직 실행 후 결과 생성  
- 서버는 결과를 **HTTP 응답 메시지**로 만들어 클라이언트에 전송  
  - 예: `HTTP/1.1 200 OK` + HTML 본문

---

## 6. 브라우저 렌더링
브라우저는 응답을 받은 뒤 다음 과정을 거쳐 화면에 출력:
1. **HTML 파싱** → DOM(Document Object Model) 트리 생성  
2. **CSS 파싱** → CSSOM(CSS Object Model) 트리 생성  
3. **JavaScript 실행** → DOM/CSSOM 수정 가능  
4. **렌더 트리(Render Tree) 생성** → 화면 표시 요소 결정  
5. **레이아웃(Layout)** → 요소 크기와 위치 계산  
6. **페인팅(Painting)** → 픽셀 단위로 화면 출력  

---

## 📊 요약 흐름도

| 단계       | 설명                          |
|------------|-------------------------------|
| URL 입력   | 사용자가 주소 입력             |
| DNS 조회   | 도메인 → IP 변환               |
| TCP 연결   | 서버와 통신 채널 생성          |
| HTTP 요청  | 브라우저 → 서버                |
| 서버 응답  | 서버 → 브라우저                |
| 렌더링     | HTML, CSS, JS 처리 후 화면 출력 |
